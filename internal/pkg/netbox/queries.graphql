query test {
  ip_address(id: 2) {
    id
    address
    description
    vrf {
      id
      name
    }
    tenant {
      id
      name
    }
    status
  }
}

query getTypes {
  __schema {
    types {
      name
    }
  }
}