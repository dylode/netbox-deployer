query GetAllTypes {
  __schema {
    types {
      name
    }
  }
}

query GetTypeInfo {
  __type(name: "IPAddressType") {
    name
    fields {
      name
      type {
        name
        kind
        ofType {
          name
          kind
        }
      }
    }
  }
}